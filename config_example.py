PATH = 'PATH json file with the username and password.'
CURRENT_TOKEN = None
PORT = #Port for app. When I run locally it is PORT = 8000
BACKEND_URL = "URL to backend API. In my case it would be http://localhost:5000/chrono_test/api/"
